import "myabi"

void main(){
  //	call_syscall00();
  //	call_syscall01();
  print_string("vinoos");
  char[256] os_version_return = get_os_version();
  print_string("os version: ");
  print_string(os_version_return);
 while(true) continue;
}
